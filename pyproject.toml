[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "deeprl-stocktrading"
version = "0.1.0"
description = "Deep reinforcement learning stock trading project"
readme = "README.md"
requires-python = ">=3.9"
